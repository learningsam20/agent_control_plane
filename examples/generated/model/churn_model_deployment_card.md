# churn_model — deployment card

> Generated by: **ml-engineer** (governed agent run `run-de60b77ae9ea`)
> Governed actions: `deploy churn_model` → **allow** (`allow-deploy-ml`, order 55 —
> elevated+ agents may deploy in the ML domain)
> Model: `churn_model` (mlflow, domain ML, classification sensitive, owner Data Science)

## What is being deployed

Gradient-boosted churn prediction model refreshed nightly from
`ml.churn_features` and served to product surfaces from `ml.churn_predictions`.

**DataHub ML lineage (end-to-end):**

```
events.raw_clickstream ─┐
                        ├─▶ ml.churn_features ─▶ churn_model ─▶ ml.churn_predictions
raw.churn_survey ───────┘
```

## Governance gate

| Check                 | Result                       |
| --------------------- | ---------------------------- |
| Signature             | Ed25519 valid (ag_ml_engineer) |
| Reputation tier       | elevated (score 72)          |
| Policy                | `allow-deploy-ml` → allow    |
| Audit event           | `request.deploy.allowed` chained |
| Telemetry             | span `agents.execute`, metric `gateway.decisions` |

## Rollout plan

1. Canary to 5% of traffic for 48h; watch p99 latency (< 400 ms target) and
   prediction drift on `ml.churn_features`.
2. Evaluate precision@10 vs the incumbent for both segments.
3. Full rollout; write back refreshed `ml.churn_predictions` nightly.
